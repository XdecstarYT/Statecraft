# Server-side only — set this in the Netlify dashboard (Site settings > Environment
# variables), never in a committed file. Read exclusively by
# netlify/functions/ai-bill-analysis.mts via process.env.GROQ_API_KEY.
# Do NOT prefix this with VITE_ — that would bundle it into client-side JS.
GROQ_API_KEY=
